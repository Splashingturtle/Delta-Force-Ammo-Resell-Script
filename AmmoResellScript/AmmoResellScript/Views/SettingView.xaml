<UserControl x:Class="AmmoResellScript.Views.SettingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:AmmoResellScript.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel Orientation="Vertical" VerticalAlignment="Center" Margin="0,5,0,0">
        <!--参数区域-->
        <Border Background="#252537" CornerRadius="5" BorderThickness="1" BorderBrush="#3A3A52">
            <StackPanel Orientation="Vertical">

                <!--按钮区域-->
                <Grid>
                    <!--行列定义-->
                    <Grid.RowDefinitions>
                        <RowDefinition/>
                        <RowDefinition/>
                        <RowDefinition/>
                        <RowDefinition/>
                        <RowDefinition/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition/>
                        <ColumnDefinition/>
                    </Grid.ColumnDefinitions>
                    
                    <!--按钮定义-->
                    <Button Content="绑定目标子弹" Style="{StaticResource CommonButtonStyle}" Height="30" Grid.Row="0" Grid.Column="0" Command="{Binding CommonButtonBindingCommand}" CommandParameter="TargetAmmo"/>
                    <Button Content="绑定200发托条" Style="{StaticResource CommonButtonStyle}" Height="30" Grid.Row="0" Grid.Column="1" Command="{Binding CommonButtonBindingCommand}" CommandParameter="TwoHundredAmmo"/>
                    <Button Content="绑定购买按钮" Style="{StaticResource CommonButtonStyle}" Height="30" Grid.Row="1" Grid.Column="0" Command="{Binding CommonButtonBindingCommand}" CommandParameter="BuyButton"/>
                    <Button Content="绑定开始游戏" Style="{StaticResource CommonButtonStyle}" Height="30" Grid.Row="1" Grid.Column="1" Command="{Binding CommonButtonBindingCommand}" CommandParameter="StartGameButton"/>
                    <Button Content="绑定交易行" Style="{StaticResource CommonButtonStyle}" Height="30" Grid.Row="2" Grid.Column="0" Command="{Binding CommonButtonBindingCommand}" CommandParameter="TradeRowButton"/>
                    <Button Content="绑定账户坐标" Style="{StaticResource CommonButtonStyle}" Height="30" Grid.Row="2" Grid.Column="1" Command="{Binding CommonButtonBindingCommand}" CommandParameter="Account"/>
                    <Button Content="绑定价格区域" Style="{StaticResource CommonButtonStyle}" Height="30" Grid.Row="3" Grid.Column="0" Command="{Binding CommonButtonBindingCommand}" CommandParameter="PriceRegion"/>
                    <Button Content="绑定剩余哈弗币" Style="{StaticResource CommonButtonStyle}" Height="30" Grid.Row="3" Grid.Column="1" Command="{Binding CommonButtonBindingCommand}" CommandParameter="RemainingHaverCoinRegion"/>
                    <Button Content="绑定烽火地带" Style="{StaticResource CommonButtonStyle}" Height="30" Grid.Row="4" Grid.Column="0" Command="{Binding CommonButtonBindingCommand}" CommandParameter="FengHuo"/>
                    <Button Content="绑定全面战场" Style="{StaticResource CommonButtonStyle}" Height="30" Grid.Row="4" Grid.Column="1" Command="{Binding CommonButtonBindingCommand}" CommandParameter="QuanMian"/>
                </Grid>

                <!--输入框区域-->

                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition/>
                        <RowDefinition/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition/>
                        <ColumnDefinition/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Orientation="Horizontal" Grid.Row="0" Grid.ColumnSpan="2" HorizontalAlignment="Left" VerticalAlignment="Center">
                        <TextBlock VerticalAlignment="Bottom" Height="35" Foreground="White" FontSize="14" Margin="0,0,10,0">
                            购买次数:
                        </TextBlock>
                        <TextBox Padding="0" Text="{Binding PurchaseCount}" Style="{StaticResource CommonTextBoxStyle}" Height="35" Width="200" FontSize="14"/>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" Grid.Row="1" Grid.ColumnSpan="2" HorizontalAlignment="Left" VerticalAlignment="Center">
                        <TextBlock VerticalAlignment="Bottom" Height="35" Foreground="White" FontSize="14" Margin="0,0,10,0">
                            识别延迟:
                        </TextBlock>
                        <TextBox Padding="0" Text="{Binding ClickDelay}" Style="{StaticResource CommonTextBoxStyle}" Height="35" Width="200" FontSize="14"/>
                    </StackPanel>
                </Grid>
                <Button Content="保存配置" Style="{StaticResource CommonButtonStyle}" Height="40" Width="90" Margin="7" HorizontalAlignment="Left" Command="{Binding SaveCommand}"/>
            </StackPanel>
        </Border>
        
        <!--输出框-->
        <TextBox Text="{Binding Logs}" Height="80" Style="{StaticResource TextBoxStyle}" Width="380" IsReadOnly="True"/>
    </StackPanel>
    
</UserControl>
