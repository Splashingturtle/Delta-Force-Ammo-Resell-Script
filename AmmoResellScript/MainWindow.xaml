<Window x:Class="AmmoResellScript.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:UserControls="clr-namespace:AmmoResellScript.UserControls"
        xmlns:chrome="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        mc:Ignorable="d"
        Title="Ammo Resell"
        Height="620" Width="600"
        MinWidth="520" MinHeight="460"
        Topmost="True"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Icon="/images/1.jpg">

    <chrome:WindowChrome.WindowChrome>
        <chrome:WindowChrome GlassFrameThickness="-1"
                             ResizeBorderThickness="6"
                             CaptionHeight="0"
                             CornerRadius="0"
                             UseAeroCaptionButtons="False"/>
    </chrome:WindowChrome.WindowChrome>

    <!-- 根 Grid：透明底 + 毛玻璃视觉层级 -->
    <Grid>
        <!-- 第0层：亚克力底色区域（叠加在系统模糊之上） -->
        <Border x:Name="AcrylicBase"
                Background="#40161B24"
                CornerRadius="0"
                SnapsToDevicePixels="True"/>

        <!-- 第1层：内容布局 -->
        <Grid>
            <Grid.RowDefinitions>
                <!-- 自定义标题栏 -->
                <RowDefinition Height="36"/>
                <!-- 导航栏 -->
                <RowDefinition Height="Auto"/>
                <!-- 主体内容 -->
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- ═══ 标题栏 ═══ -->
            <Grid Grid.Row="0"
                  Background="{StaticResource GlassTitleBarBg}"
                  MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- 拖拽区 + 标题 -->
                <StackPanel Grid.Column="0" Orientation="Horizontal"
                            VerticalAlignment="Center" Margin="14,0,0,0">
                    <TextBlock Text="Ammo Resell"
                               FontSize="12" FontWeight="SemiBold"
                               Foreground="{StaticResource TextPrimary}"
                               VerticalAlignment="Center"/>
                </StackPanel>

                <!-- 窗口控制按钮 -->
                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <!-- 最小化 -->
                    <Button Width="36" Height="28"
                            Background="Transparent" BorderThickness="0"
                            Cursor="Hand"
                            Click="Minimize_Click"
                            ToolTip="最小化">
                        <TextBlock Text="─" FontSize="13"
                                   Foreground="{StaticResource TextMuted}"
                                   HorizontalAlignment="Center"
                                   VerticalAlignment="Center"/>
                    </Button>
                    <!-- 最大化 -->
                    <Button x:Name="MaxBtn" Width="36" Height="28"
                            Background="Transparent" BorderThickness="0"
                            Cursor="Hand"
                            Click="Maximize_Click"
                            ToolTip="最大化">
                        <TextBlock Text="□" FontSize="13"
                                   Foreground="{StaticResource TextMuted}"
                                   HorizontalAlignment="Center"
                                   VerticalAlignment="Center"/>
                    </Button>
                    <!-- 关闭 -->
                    <Button Width="36" Height="28"
                            Background="Transparent" BorderThickness="0"
                            Cursor="Hand"
                            Click="Close_Click"
                            ToolTip="关闭">
                        <TextBlock Text="✕" FontSize="13"
                                   Foreground="{StaticResource TextMuted}"
                                   HorizontalAlignment="Center"
                                   VerticalAlignment="Center"/>
                    </Button>
                </StackPanel>
            </Grid>

            <!-- ═══ 导航栏 ═══ -->
            <UserControls:Navigation Grid.Row="1"/>

            <!-- ═══ 主体内容 ═══ -->
            <ContentControl x:Name="ContentArea" Grid.Row="2"
                            Content="{Binding MainContent}"/>
        </Grid>
    </Grid>
</Window>
